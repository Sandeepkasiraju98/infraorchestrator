# InfraOrchestrator – Autonomous Cloud Infra Agent (MVP)

This is an MVP that turns a natural-language infra prompt into Terraform files,
runs basic policy & cost checks, and prints a plan (no apply by default).
